@model IEnumerable<HallBooking.Models.Book>
@{
    Layout = "~/Views/Shared/Admin.cshtml";
}

<link href="https://cdn.datatables.net/1.11.1/css/jquery.dataTables.min.css" rel="stylesheet" />
<link href="https://cdn.datatables.net/buttons/2.0.0/css/buttons.dataTables.min.css" rel="stylesheet" />


<table class="table" style=" margin:0px" id="t">
    <thead>
        <tr>
            <th>
                User Name
            </th>
            <th>
                Hall
            </th>
            <th>
                Date from
            </th>
            <th>
                Date to
            </th>
            <th>
                Price
            </th>
            <th>
                Status
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            @if (item.Status == "Under Process" || item.Status == "Accept"|| item.Status == "Reject")
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.User.Fullname)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Hall.Hallname)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Startdate)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Enddate)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Hall.Price)
                    </td>
                    @if (item.Status == "Under Process")
                    {
                        <td>
                            Under Process

                        </td>
                    }
                    else if (item.Status == "Reject")
                    {
                        <td>
                            Reject

                        </td>
                    }
                    else if (item.Status == "Accept")
                    {
                        <td>
                            Accept

                        </td>
                    }
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.Id">Edit status</a>
                    </td>
                </tr>}
            }

        </tbody>
</table>
<script src="https://code.jquery.com/jquery-3.5.1.js"></script>

<script src="https://cdn.datatables.net/1.11.1/js/jquery.dataTables.min.js" defer></script>

<script src="https://cdn.datatables.net/buttons/2.0.0/js/dataTables.buttons.min.js" defer></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/jszip/3.1.3/jszip.min.js"></script>

<script src="https://cdnjs.cloudflare.com/ajax/libs/pdfmake/0.1.53/pdfmake.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/pdfmake/0.1.53/vfs_fonts.js"></script>
<script src="https://cdn.datatables.net/buttons/2.0.0/js/buttons.html5.min.js" defer></script>
<script>
    $(document).ready(function () {
        $('#t').DataTable({
            dom: 'Bfrtip',
            buttons: [
               
            ]
        });
    });
</script>